{
  "active": true,
  "iteration": 1,
  "minIterations": 1,
  "maxIterations": 1,
  "completionPromise": "TASK_COMPLETE",
  "tasksMode": false,
  "taskPromise": "READY_FOR_NEXT_TASK",
  "prompt": "# Goal: Make Allura deployable, truthful, and provably ready\n\n**Goal ID:** goal-20260613-0414\n**Stopping condition:** ALL four true and verifiable:\n1. Fresh `docker compose up` on a clean machine → every container healthy; `GET /api/health/live` and gateway `/ready` return 200; transcript recorded and `docs/archive/allura/INSTALL-DEPLOY-REVIEW.md` flipped to \"verified\".\n2. `bun run test:e2e` runs the 10-point acceptance gate against REAL Postgres + Neo4j (not mocks), including resume-after-approval and crash-restart with no duplicate side effects — all green.\n3. Dashboard truthfulness audit: zero hardcoded/static claims (proposals count, embedding dims, Teams roster); Team Durham read-only evidence stored under `group_id=allura-team-durham`.\n4. A superseding readiness report records no critical planning gaps (Sprint Change Proposal criterion 5).\n\n**Guardrails:**\n- Invariants: `group_id` on every DB op; PostgreSQL append-only; Neo4j `SUPERSEDES`; HITL before promotion; `allura-*` tenant only.\n- NO desktop packaging / AionUi work (Path B is human/Captain-reserved).\n- NO deciding business requirements (Faith Meats) — human-only.\n- Bun only (npm/npx banned). No net-new files in `docs/allura/` beyond the canonical six.\n- No legacy tenants (`allura-roninmemory` / `allura-team-ram`).\n- Do NOT route to OpenClaw. Engine work → Team RAM; UX → Team Durham.\n\n**Created:** 2026-06-13T04:15Z\n**Brain trace:** 22540e2d-158f-492e-bae3-55a4e457cb7b\n\n## Tasks\n\n### Sprint A — Make it deployable (Hightower)\n- [ ] A1. Add a first-run setup script that idempotently creates the external `knowledge-network` and the three external volumes (`memory_postgres_data`, `neo4j_data`, `neo4j_logs`) before `docker compose up`. Verify: `bash scripts/validate-env.sh` passes and the script is safe to re-run.\n- [ ] A2. Perform a clean fresh deploy (`build --no-cache` → `up -d` → `docker compose ps`), confirm all containers reach healthy, and `curl -f` both `http://localhost:3100/api/health/live` and gateway `/ready` return 200. Capture the full transcript.\n- [ ] A3. Update `docs/archive/allura/INSTALL-DEPLOY-REVIEW.md` — replace \"FRESH DEPLOY NOT VERIFIED\" with the verified transcript and date.\n\n### Sprint B — Make it true & provable (Team RAM + Team Durham)\n- [ ] B1. Write a live-DB E2E that exercises the full 10-point acceptance gate against real Postgres + Neo4j (start → parallel tasks → block at approval → resume → complete → quality gates → evidence packet → close work item → memory writeback → restart-and-reconstruct). Verify with `bun run test:e2e`.\n- [ ] B2. Add idempotency + crash-restart coverage to the E2E: approve/resume twice yields exactly one set of side effects; kill mid-run, restart, run completes with no duplicate evidence packet.\n- [ ] B3. Fix the CLI `--group-id` so it actually scopes execution (not metadata-only); add a test proving a wrong-tenant run is refused.\n- [ ] B4. Replace every static dashboard claim with live / unknown / degraded state (proposals count, embedding dims, Teams roster, scheduled-tasks/dreams status). Each operational response declares tenant, source, freshness, degraded.\n- [ ] B5. Team Durham read-only truthfulness audit on the running dashboard — screenshots + drift evidence vs brand-truth, stored under `group_id=allura-team-durham`; route any fixes to Glaser/Rand.\n\n### Sprint C — Make it shippable & honest (Team RAM)\n- [ ] C1. Give the dashboard a real production build: multi-stage `Dockerfile.dashboard` (build → run built app) and set Next.js standalone output; compose runs the built app in `NODE_ENV=production`.\n- [ ] C2. Reconcile canonical docs in the same PR as the code: `DATA-DICTIONARY.md` (RunRecord is implemented), `REQUIREMENTS-MATRIX.md` (F49-F52), retire stale 06-12 audit claims; add AI-disclosure where drafted.\n- [ ] C3. Write the superseding readiness report (records no critical planning gaps); supersede the 06-12 audit; write a governed outcome trace summarizing the goal completion.\n\n## Completion Check\n\n```bash\nbun run typecheck && bun test          # logic green\nbun run test:e2e                       # 10-point gate vs real Postgres+Neo4j\ndocker compose --env-file .env --env-file .env.local up -d && docker compose ps\ncurl -f http://localhost:3100/api/health/live && curl -f http://localhost:5888/ready\n```\nPlus: INSTALL-DEPLOY-REVIEW.md == verified, Durham truthfulness evidence stored, superseding readiness report written.\n",
  "startedAt": "2026-06-13T04:33:24.613Z",
  "model": "",
  "agent": "opencode"
}