# Team / enterprise overrides for _bmad/config.toml.
# Committed to the repo — applies to every developer on the project.
# Tables deep-merge over base config; keyed entries merge by key.
# Example: override an agent descriptor, or add a new agent.
#
# [agents.bmad-agent-pm]
# description = "Prefers short, bulleted PRDs over narrative drafts."

# Keep BMad planning and implementation artifacts in the tracked locations
# used by this repository. `_bmad-output/` remains an ignored scratch surface
# for transient core/TEA output only.
[modules.bmm]
planning_artifacts = "{project-root}/_bmad/bmm/planning"
implementation_artifacts = "{project-root}/_bmad/bmm/stories"
project_knowledge = "{project-root}/docs"
