# Team / enterprise overrides for _bmad/config.toml.
# Committed to the repo — applies to every developer on the project.
# Tables deep-merge over base config; keyed entries merge by key.
# Example: override an agent descriptor, or add a new agent.
#
# [agents.bmad-agent-pm]
# description = "Prefers short, bulleted PRDs over narrative drafts."

# Keep durable planning and implementation artifacts under one tracked output
# root. Other generated output remains ignored. Overrides survive reinstalls.
[core]
output_folder = "{project-root}/_bmad_output"

[modules.bmm]
planning_artifacts = "{project-root}/_bmad_output/planning-artifacts"
implementation_artifacts = "{project-root}/_bmad_output/implementation-artifacts"
project_knowledge = "{project-root}/docs"

[modules.tea]
test_artifacts = "{project-root}/_bmad_output/test-artifacts"
test_design_output = "_bmad_output/test-artifacts/test-design"
test_review_output = "_bmad_output/test-artifacts/test-reviews"
trace_output = "_bmad_output/test-artifacts/traceability"
