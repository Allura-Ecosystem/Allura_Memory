# BMAD Project Configuration — Allura Memory
project_code: "allura-memory"
project_name: "Allura Memory Operations Phase 2"

# Config
output_folder: "{project-root}/_bmad-output"
planning_artifacts: "{project-root}/_bmad-output/planning-artifacts"
project_knowledge: "{project-root}"

# BMM module config
bmm:
  project_name: "Allura Memory Operations Phase 2"
  project_version: "0.3.0"
  project_code: "allura-ops-p2"
