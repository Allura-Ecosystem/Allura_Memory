---
epic: 25
date: 08-28-2026 17:45
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Epic 25 Retrospective — Governed Curator Console / Module Registry Reconciliation

## Epic summary

- **Epic:** 25 — Governed Curator Console: Module Registry Reconciliation
- **Diff range:** `bf7a8d3e^..HEAD` (70 commits, 12 merges measured; range includes interleaved Epic 24/26 work merged during the same window — Epic-25-attributed churn isolated per story below)
- **Stories completed (4/4):**
  - 25.2a — Workspace Scope and Evidence Lifecycle Foundation (`bf7a8d3e`, PR #105)
  - 25.2b — Authenticated Session Entry Point (`adfb2663`, PR #106; reviewed in `25-2b-code-review.md`, APPROVE)
  - 25.3a — Curator Read Contract and Workspace Authority Remediation (`bff4f845`, PR #122)
  - 25.3b — Modular Dashboard Workflow Contract Registry (`d25a1f6e`, PR #124; acceptance review 2026-08-28, remediation `c1516812` + `9dfe12b9` + `a1c7282b`)
- **Pending stories:** none (`detect-epic --epic 25` → `pending_stories: []`)
- **Declared acceptance gate (epic spec `epic-25-governed-curator-review-console.md:49`):** "Story 25.3b receives canonical independent-acceptance/status reconciliation before it advances to done." — **met** 2026-08-28 via the bmad-code-review acceptance pass and status reconciliation (`25-3b-code-review.md`).
- **Evidence inventory:** epic spec ✓, 4 story files ✓, per-story review records (25.2b, 25.3b) ✓, sprint-status ✓, git evidence pre-pass ✓, session logs (this session's transcripts) ✓ partial — full unattended-run session logs for 25.2a/25.3a not retained; process-lesson analysis for those stories is narrowed accordingly.

## Findings

### F1 — Cross-story authority seam: registry trusts the 25.2b header seam (accepted by design)
The registry derives all scope from `getAuthUser(request)` parsing `x-allura-*` headers (`module-registry.ts:99-103`); anti-forgery enforcement lives in the 25.2b middleware seam, not in this diff (`story-25-2b-auth-entry.test.ts`). The acceptance audit flagged this (finding #6); disposition: the seam is server-owned and overwritten by middleware before the registry sees it, and the executing middleware tests pin the overwrite. **Accept as-is** — documented in `25-3b-code-review.md`.

### F2 — Dead code: `assertCapabilities`/`assertCompatible` have no production callers
`src/lib/bumblebee/module.ts:42-54` — both functions are pinned only by `module.test.ts`; the registry implements its own validation. **Defer** (tracked; harmless, defensive contracts).

### F3 — Integrity hash shadowed by reference-identity check
`validateManifest` (`module-registry.ts:73-86`) throws on `manifest !== trusted` before the `manifestHash` comparison can fire; the hash still feeds audit metadata (`manifest_sha256`). **Accept as-is** — identifier check is strictly stronger; noted in the review record.

### F4 — Sprint-status has two sources of truth that drifted (fixed today)
`development_status` bridge map (sprint-status.yaml:571+) duplicated story statuses and drifted from the `stories[]` array — 25.3b was `review` in the bridge while `done` in the array until reconciled today (`1c0fffb4`). sprint-status.yaml was churned across 18 commits (745+/397−) in the range. The BMad tooling (`sprint_plan.py`) reads the bridge; hand-edits bypass it. **Fix-now (process):** status updates go through the bundled scripts only; the bridge is regenerated on each script run, never hand-edited.

### F5 — Epic-24 retrospective key marked done with no retro document
`development_status["epic-24-retrospective"] = done` but no `epic-24-retrospective.md` exists in `_bmad/bmm/stories/` (only epic-18..23 have documents). **Defer** (tracked) — either produce the doc or mark the key honestly.

### F6 — Review-process observation: one of four review layers timed out
Blind Hunter hit the 90s API timeout during the 25.3b review; three layers completed and produced findings. Not a defect in the epic — noted as a harness-capacity observation for future review dispatches.

## Behavior verification

- Full unit lane: **2,252 passed | 160 skipped** (post-remediation).
- Executing page-wiring test `curator-handoff-page.test.tsx` runs the real `/dashboard/curator` server component: issued-shell (`complete` + host-owned adapter + ledger decision), disabled (`degraded` + truthful copy), unauthenticated (redirect, no ledger event).
- CI on `develop` post-remediation pushes: **8/8 green** across the final run batch.
- Story-level evidence from the story records: 25.2a/25.3a merged with Pike/Fowler/Knuth sign-offs and live-DB lanes, per their status evidence.

## Previous-retro follow-through

**Nothing to follow through on.** `sprint-status.yaml` carries no `action_items` block, and no epic-24 retro document exists to carry forward items (see F5). Recorded as "no action_items block present" — not "no outstanding items", since F5 shows the epic-24 retro key is marked done without an artifact.

## Action items

1. **AI-1 (process, owner Brooks):** Update epic spec `epic-25-governed-curator-review-console.md` status lines (9, 39-42, 49) to reflect 25.3b done + acceptance reconciled. *(Proposed spec reconciliation — awaiting human application.)*
2. **AI-2 (process, owner Brooks):** Sprint-status updates use `sprint_plan.py` / `sprint_status.py` only; never hand-edit the file (F4). Add a guard note to the repo's `project-context.md` or AGENTS.md. *(Proposed process change.)*
3. **AI-3 (owner Woz, deferred):** Remove or wire `assertCapabilities`/`assertCompatible` dead code in `src/lib/bumblebee/module.ts` (F2).
4. **AI-4 (owner Woz, deferred):** Rename `module-registry.audit-failure.test.ts` title — it targets an unreachable `manifest_invalid` branch (the test actually exercises `appendDecision` write rejection) (F3/F5 review record).
5. **AI-5 (owner Fowler, deferred):** Produce or honestly mark the epic-24 retrospective (F5) — no document exists despite the key being `done`.

## Acceptance verdict

**accepted-with-open-items** (criteria: declared).

- The epic's single declared gate (25.3b independent acceptance reconciliation before done) is **met** (2026-08-28, `25-3b-code-review.md` APPROVE).
- All four stories are `done`; `pending_stories` is empty.
- No blocking findings. Open items are the deferred process/hygiene items above (F2/F3/F5) and the two proposed process changes (F4), all tracked as action items with owners.

## Open questions

- Whether the epic-24 retrospective should be produced retroactively or its key marked honestly (F5) — affects AI-5 scope.
- Whether AGENTS.md or project-context.md should carry the "scripts-only for sprint-status" guard (AI-2) — proposal pending.

## Assumptions

Interactive run — no headless assumptions recorded. The diff range includes interleaved Epic 24/26 commits merged in the same window; Epic-25 findings are attributed per-story and cross-story boundaries only, not to the full-range churn.
