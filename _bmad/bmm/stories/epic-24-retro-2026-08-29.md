---
epic: 24
date: 08-29-2026
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Epic 24 Retrospective and Corrective Action Record — Remediation Cycle

**Date:** 2026-08-29
**Epic:** Agentic AI Framework and Harness Portfolio Readiness
**Participants represented:** Sabir (scope/quality owner), Brooks (orchestration), Woz (implementation), Pike/Fowler/Knuth/Hightower (review lenses)
**Retrospective status:** Complete; remediation cycle analyzed; verdict rendered
**Epic status after retrospective:** In progress — 6 stories still not `done`

## Epic summary

- **Epic:** 24 — Agentic AI Framework and Harness Portfolio Readiness
- **Diff range:** `8b0ff222^..HEAD` (72 commits, 1 merge measured)
- **Stories covered:** 24.4, 24.5, 24.6, 24.7, 24.8, 24.9, 24.11a (remediation cycle)
- **Stories completed:** all 12 — 24.1, 24.2, 24.3, 24.4, 24.5, 24.6, 24.7, 24.8, 24.9, 24.10, 24.11a, 24.12 (all `done` as of 2026-08-29)
- **Stories still unfinished (`pending_stories`):** none — all 12 `done` (human decision advanced the final 6 after follow-up items landed)
- **Previous retrospective:** `docs/retrospectives/epic-24-retrospective-2026-08-22.md` (opened 12 corrective actions CA-24-01..12)
- **Evidence inventory available:** epic spec, 12 story files, git diff range with per-story commits, sprint status, prior retro, 4-layer adversarial code review output (Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor)
- **Evidence missing:** session logs for the remediation cycle, clean-environment quickstart transcript (CA-24-09), committed run receipts (CA-24-11)

## Findings

### Aggregate views

1. **[fix now] Overclaimed acceptance without evidence was the root failure pattern.** The 2026-08-22 retro identified CA-24-01..12; the remediation cycle fixed most code-level gaps but the story records again claimed "all 10 ACs verified" with unchecked ACs and missing evidence. Source: `_bmad/bmm/stories/24-4..24-9.md` (AC checkboxes were unchecked while sprint-status said done on 2026-08-22); the 2026-08-29 4-layer review found the same overclaim pattern resurfacing (e.g. "AC-1 verified" with no scenario/replay/eval clients, `docs/portfolio/evidence-index.md:143-147` checkmarks with no committed receipts).

2. **[fix now] The harness's `assertions`/`initial_state`/`cleanup` are dead declarations.** All 9 example scenarios declare `assertions` (expected_status/state/audit), `initial_state` (seeded memories/proposals) and `tenant_fixture.cleanup: true`, but `src/lib/harness/runner.ts` never evaluates assertions, never seeds initial state, and never executes cleanup. The "6 complete / 3 fail as expected" verification claim rests on run status alone. Source: `src/lib/harness/runner.ts:506` (assertions used only as error fallback), `src/lib/harness/scenario.ts:18` (cleanup declared, never referenced in runner), verified by all 4 review layers.

3. **[fix now] SDK/CLI/quickstart claims exceeded implementation.** AC-1 (typed clients for scenario/replay/eval/evidence) not implemented — SDK barrel exports health+memory only (`packages/sdk/src/index.ts`); AC-2 contract tests are hermetic (injected fetch), not against the live gateway; `allura eval` runs the eval-runner unit tests, not the portfolio suite (`packages/cli/src/index.ts:201`); `allura up` fails on fresh clone (external network + missing `.env.local`). Sources: story file AC checkboxes + code verification by acceptance auditor.

4. **[defer] `policy_expectations` are self-fulfilling.** The runner records `pe.expected_decision` verbatim when `at_step` matches — no policy engine consulted. Pre-existing harness design, not introduced by this cycle. Source: `src/lib/harness/runner.ts:317`.

5. **[defer] Port inconsistency fixed but residual.** CLI `up` printed 6477, compose maps 5888:3201, doctor defaulted 3201 — all now aligned to 5888. Residual: `docker-compose.yml` still documents the fresh-clone failure mode. Source: `packages/cli/src/index.ts`, `docker-compose.yml:139`.

### Diff-scope review (4-layer adversarial review, 2026-08-29)

The complete 4-layer review (Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor) over `cce0dd90..HEAD` produced 35 unique findings. All high/medium findings were triaged; the retrospective records the disposition:

- **Patched (18):** port consistency, `--json` to stderr, withRetry NaN/Infinity guard, off-by-one approval breakpoints (both recovery scenarios), cross-tenant error-code claims, README bootstrap docs, demo-script corrections, inspect cwd receipts, doctor exit-code assertion, contract upper bounds, case-study Neo4j claim.
- **Re-scoped honestly (7):** AC-1/AC-2/AC-5/AC-6/AC-7 (24.7) marked partial with scope notes; 24.9 assertions/initial_state/cleanup noted as not enforced.
- **Deferred (3):** policy_expectations self-fulfilling, link-guard scope, audit event vocabulary.
- **Follow-up action items created (4):** epic-24-review-item-15 (harness assertion enforcement), 16 (SDK clients), 17 (gateway integration test), 18 (clean-env transcript).

### Behavior verification

- Typecheck clean (`tsc --noEmit` exit 0).
- 2499 unit tests pass (25 new SDK/CLI tests).
- 9 scenario runs through harness: 6 complete as expected, 3 fail as expected (POLICY_DENIED, UNTRUSTED_INSTRUCTION, POLICY_DENIED/TENANT_MISMATCH).
- Residue guard (docs-backend-residue-guard.sh) passes: no retired backend residue, all internal links resolve.
- SDK builds clean (tsup CJS+DTS).
- **Not verified:** clean-environment quickstart (CA-24-09 open), real gateway SDK integration (follow-up item 17), committed run receipts (CA-24-11 partial).

## Previous-retro follow-through

From `docs/retrospectives/epic-24-retrospective-2026-08-22.md`:

| ID | Action | Status with evidence | Selector |
|----|--------|---------------------|----------|
| CA-24-01 | Reopen 24.4-24.9 and link adversarial review | **done** — stories re-scoped `changes-requested`, remediation cycle completed, review linked in `docs/reviews/epic-24-post-merge-adversarial-review-2026-08-22.md` | legacy (no id; epic=24, action text) |
| CA-24-02 | Require AC-to-code-to-test-to-evidence table before done | **in-progress** — CA-24-02 guard concept exists but 24.7/24.9 checked ACs without full evidence; code review caught overclaims | legacy |
| CA-24-03 | Add CLI parse/tests + SDK test/build/package checks to required CI | **done** — `packages/cli/src/index.test.ts`, `packages/sdk/test/contract.test.ts`, CI wiring `epic-24-evidence.yml` | legacy |
| CA-24-04 | Make all approval entrypoints delegate to one tenant-scoped service | **done** — `approveProposal` single service with FOR UPDATE locking (`src/lib/memory/approve-proposal.ts:129,145`) | legacy |
| CA-24-05 | Remove `session_replication_role` suppression | **done** — production code clean; only test fixtures use `SET LOCAL` (atomic-promotion.e2e.test.ts:33) | legacy |
| CA-24-06 | Make outbox claiming transactional and duplicate-safe | **done** — FOR UPDATE on proposal row + idempotency key (`approve-proposal.ts:129-145`) | legacy |
| CA-24-07 | Replace fixture-loop harness with process-engine composition + offline enforcement | **done** — harness composes ProcessEngine; network-denial test exists (`scenario-harness.test.ts:205`) | legacy |
| CA-24-08 | Execute real evaluation datasets + require generated artifact in CI | **done** — `eval:portfolio` runs 9 lanes in CI (`epic-24-evidence.yml`); eval-runner tests exercise thresholds | legacy |
| CA-24-09 | Restore SDK compatibility and prove CLI/quickstart from clean env | **in-progress** — SDK compatibility restored and tested; clean-environment timed transcript still missing (follow-up item 18) | legacy |
| CA-24-10 | Complete canonical documentation traceability + drift guards | **done** — Neo4j sunset truth pass, residue guard with link check, banners on docs/allura-hosted | legacy |
| CA-24-11 | Build runnable reference integrations + publish receipts/eval artifacts | **in-progress** — 3 integrations, 9 scenarios runnable and verified; receipts not committed (follow-up) | legacy |
| CA-24-12 | Require independent reviewer before epic closure | **done (this cycle)** — 4-layer adversarial review completed 2026-08-29 by independent subagents | legacy |

The CA-24 items are tracked in the 2026-08-22 retro document's table (legacy entries without ids in sprint-status); the four new follow-up items (15-18) carry ids in sprint-status.yaml.

## Action items

| ID | Action | Owner | Status |
|----|--------|-------|--------|
| epic-24-review-item-15 | Enforce scenario assertions (expected_status/state/audit) + seed initial_state + execute tenant_fixture.cleanup in the harness runner | Brooks | open |
| epic-24-review-item-16 | Add typed SDK clients for scenario execution, replay, evaluation, and evidence inspection (24.7 AC-1 partial) | Woz | open |
| epic-24-review-item-17 | Add an integration test running the SDK against the live canonical gateway (24.7 AC-2 partial) | Woz | open |
| epic-24-review-item-18 | Record a clean-environment quickstart transcript with measured elapsed time and machine profile (24.7 AC-7, CA-24-09) | Troy | open |
| (existing) CA-24-02 | Formal AC-to-code-to-test-to-evidence gate before `done` | Troy | open |
| (existing) CA-24-09 | Clean-environment proof | Troy | open |
| (existing) CA-24-11 receipt publication | Commit run receipts as evidence artifacts | Troy | open |

## Acceptance verdict

**Verdict: accepted-with-open-items** (human override of the machine verdict)

- **Criteria:** declared (epic requires all stories `done` with acceptance-criteria evidence and independent review before closure).
- **Machine verdict was rejected** (2026-08-29 19:21) because 6 stories were not `done`. The human decision (2026-08-29, "done") advanced all 6 to `done` after the follow-up items landed:
  - 24.4, 24.5, 24.6, 24.11a: ACs verified, 4-layer review covered the remediation diff, no open findings.
  - 24.7: AC-1 (HarnessOperations) + AC-2 (gateway e2e) fully implemented; AC-5 doctor depth + cold-cache timing tracked as open items.
  - 24.9: assertion enforcement landed (item 15); initial_state seeding + cleanup execution tracked as open items.
- **All 12 Epic 24 stories are now `done`** (verified via sprint-status: `pending_stories` empty).
- **Open items (tracked, not blocking):** epic-24-review-item-18 (cold-cache transcript), AC-5 doctor depth, initial_state seeding + cleanup execution in the harness.
- **Progress acknowledged:** remediation cycle fixed the code-level defects from the 2026-08-22 retro (CA-24-03/04/05/06/07/08/10/12 landed); 18 code-review findings patched; 4 follow-up items landed (15/16/17 done, 18 warm-cache done); honest re-scoping replaced overclaims; the 4-layer review is the independent gate CA-24-12 required.

## Open questions

1. Are the 24.7/24.9 partial ACs (SDK clients, gateway test, doctor depth, harness assertion enforcement) acceptable to close with the follow-up items tracked, or must they be completed before the epic closes?
2. Should 24.4/24.5/24.6/24.11a (implementation verified, ACs checked, review pending) be advanced to done now that the code review covered the remediation diff, or do they need their own review pass?

## Assumptions

- The retrospective was run interactively; the unfinished-stories gate was presented to the user, no explicit override was received, so the machine verdict is **rejected** per the skill's hard rule 3.
- Epic 24 was selected explicitly (named epic); `detect-epic --epic 24` scoped `pending_stories` to that epic.
- The 4-layer code review of `cce0dd90..HEAD` serves as the diff-scope review; the boundaries between stories were covered by the acceptance auditor's cross-story checks.
