# Troy Contract Readiness

> Date: 2026-06-01 03:31 EDT
> Scope: `/api/events`, `/api/contracts`, `/api/promotions`, `/api/policies`, `/api/memory/lineage`
> Mode: contract truth review only. No implementation or visual work.

## Troy Curator Verdict

**Status:** WATCH / BLOCKED BY CONTRACT DECISIONS

**Receipts:**
- `src/lib/dashboard/adapters.ts` declares all five required endpoints as missing adapter contracts.
- `src/__tests__/dashboard-contracts.test.ts` asserts those five endpoints remain degraded and must not render as live.
- `src/lib/dashboard/contracts.ts` marks `/api/contracts`, `/api/promotions`, `/api/policies`, and `/api/memory/lineage` dashboard surfaces degraded until adapters exist.
- Existing live source routes include `src/app/api/audit/events/route.ts`, `src/app/api/curator/proposals/route.ts`, `src/app/api/agents/route.ts`, `src/app/api/memory/graph/route.ts`, `src/app/api/memory/traces/route.ts`, and `src/app/api/health/isolation/route.ts`.
- `docs/allura/DATA-DICTIONARY.md`, `docs/allura/DESIGN-ALLURA.md`, `docs/allura/REQUIREMENTS-MATRIX.md`, and `docs/allura/RISKS-AND-DECISIONS.md` define most underlying store contracts, but not every requested dashboard adapter schema.
- Allura Brain search was degraded for this audit (`graph_unavailable`), so Allura Brain references below are marked from repo docs/code/local OpenClaw memory only.

**Boundary:** Do not mark degraded adapters complete. A route can become live only when schema, auth boundary, and source/persistence behavior are known.

## Readiness Table

| Route | Current status | Current HTTP behavior | Source-of-truth candidate | Existing repo types/adapters | Existing Notion reference | Existing Allura Brain reference | Minimum viable schema | Auth boundary | Read/write behavior | Degraded fallback behavior | Tests required | Sabir decision required |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `/api/events` | **Blocked / safe read-only alias possible** | No route file found at `src/app/api/events`; Next route should 404 when runtime serves this tree. Live curl from this session could not connect to localhost:3100, so current HTTP was inferred from route absence. | PostgreSQL `events` table, with existing `/api/audit/events` as the authoritative audit export route. | `src/app/api/audit/events/route.ts`; `src/lib/audit/query-builder.ts`; `src/lib/audit/export.ts`; `src/lib/events/event-types.ts`; `src/lib/postgres/schema/traces.sql`; dashboard adapter entry in `src/lib/dashboard/adapters.ts`. | Indirect: `notion_sync_pending` events and proposal sync pipeline reference events; no dedicated Notion events contract found. | Repo docs define events as the append-only primary log. Brain MCP search degraded during audit, so no live semantic confirmation. | `data.events[]`, `pagination`, `degraded`, `warnings`, `source`, `freshness`, `group_id`. Event row: `id`, `group_id`, `agent_id`, `event_type`, `status`, `created_at`, `metadata`; optional `workflow_id`, `parent_event_id`, `outcome`, `error_code`, `error_message`. | `viewer` for GET/export parity with `/api/audit/events`; `curator` or higher only if future event mutation/admin views appear. `group_id` validated with `validateGroupId`. | GET only for MVP. No writes. Existing append-only writes stay in trace/memory/curator services. | If PostgreSQL unavailable, return non-green degraded response with empty `events`, warning, and no fabricated counts. Do not silently alias missing data to zero. | Route unit tests for 401/403, invalid `group_id`, filters, pagination, CSV parity decision if included, PG failure degraded response, and no mutation methods. Dashboard contract test should flip only after route exists. | **Yes.** Decide whether `/api/events` is a thin alias of `/api/audit/events` or a normalized event feed with audit export as a sub-view. |
| `/api/contracts` | **Blocked** | No route file found at `src/app/api/contracts`; inferred 404. | `.opencode/agent/**` as file roster, `src/lib/agents/agent-manifest.ts` as RAM manifest, `src/lib/adapter-registry/**` for adapter policy shape, plus existing `/api/agents` for roster/activity only. | `src/app/api/agents/route.ts`; `src/lib/adapter-registry/types.ts`; `src/lib/adapter-registry/registry.ts`; `src/lib/auth/roles.ts`; dashboard adapter entry in `src/lib/dashboard/adapters.ts`. | No dedicated Notion contract reference found. Notion Work Board adapter exists, but it is not an agent contract source. | Local memory says RAM owns repo implementation/data contracts and TALON reviews API/runtime; no live Brain confirmation due degraded search. | `data.contracts[]`, `degraded`, `warnings`, `source`, `freshness`, `group_id`. Contract row should minimally include `agent_id`, `display_name`, `runtime`, `team`, `source_path`, `model`, `skills`, `read_policy`, `write_policy`, `allowed_tools`, `route_policy`, `drift_status`, `evidence_refs`. | Read: `viewer` if contract metadata is non-secret; likely `curator`/`admin` for allowed tools, model policy, and drift details. Write: none for MVP. | GET only for MVP. No config edits, no allowlist mutation, no generated policy claims. | If files are unreadable or policy source missing, return degraded contract rows with `drift_status: "unknown"` and explicit warning. Do not turn missing policy into permissive defaults. | Tests for file parsing, symlink-safe traversal if delegated paths are included, auth, no secrets in response, group scoping, missing source warnings, and adapter registry shape validation. | **Yes.** Define what counts as a contract, which source wins on conflicts, and whether sensitive permissions/tools are visible to `viewer` or only `curator/admin`. |
| `/api/promotions` | **Partial source truth / read-only MVP possible** | No route file found at `src/app/api/promotions`; inferred 404. Existing `/api/curator/proposals` works as proposal listing, not full promotion history. | PostgreSQL `canonical_proposals` plus `events` rows `proposal_*`, `memory_promoted`, `promotion_failed`, and Notion sync fields on proposals. | `src/app/api/curator/proposals/route.ts`; `src/app/api/curator/approve/route.ts`; `src/app/api/curator/reject/route.ts`; `src/lib/memory/approval-audit.ts`; `src/lib/memory/knowledge-promotion.ts`; `json-schema/canonical_proposals.schema.json`; dashboard adapter entry. | Strong: `canonical_proposals.notion_page_id`, `notion_synced_at`, `notion_sync_pending`, `src/curator/notion-sync-worker.ts`, `src/curator/notion-bridge.ts`. Notion is a projection/reference, not source truth. | Repo docs define HITL promotion queue and approval events. Brain MCP search degraded during audit. | `data.promotions[]`, `pagination`, `degraded`, `warnings`, `source`, `freshness`, `group_id`. Row: `proposal_id`, `group_id`, `content`, `score`, `tier`, `status`, `trace_ref`, `created_at`, `decided_at`, `decided_by`, `rationale`, `witness_hash`, `notion_page_id`, `notion_synced_at`, `promotion_event_id`, `memory_id` when known. | GET: `viewer` may match `/api/curator/proposals`; mutations must remain `curator` or `admin` and require governance receipt. | MVP should be GET/read-only promotion history. Do not wire approve/reject controls through `/api/promotions` until mutation receipt schema is approved. | If Neo4j or Notion unavailable, keep Postgres proposal history live but mark graph/notion projection fields degraded/unknown. If Postgres unavailable, fail degraded without fake history. | Tests for list filters (`status`, dates, proposal id), joins to approval events, `group_id`, auth, degraded Notion/Neo4j projections, no mutation methods for MVP, and fixture coverage for pending/approved/rejected/failed. | **Yes for mutation controls; no for read-only MVP if scoped to existing proposal/history fields.** Sabir must approve whether `/api/promotions` is history-only or also owns approval/rejection/revocation commands. |
| `/api/policies` | **Blocked** | No route file found at `src/app/api/policies`; inferred 404. Existing `/api/health/isolation` is only partial isolation status. | Split sources: `process.env.PROMOTION_MODE`, `AUTO_APPROVAL_THRESHOLD`, `src/lib/auth/roles.ts`, `src/lib/retrieval/policy.ts`, RuVix kernel rule docs, `events` rows with `kernel_rule` / policy decisions, and health isolation status. | `src/app/api/health/isolation/route.ts`; `src/lib/auth/roles.ts`; `src/lib/retrieval/policy.ts`; `src/lib/memory/memory-coordinator.ts`; `docs/allura/SOLUTION-ARCHITECTURE.md` RuVix rules; dashboard adapter entry. | No dedicated Notion policy API reference found. Notion may receive approved proposal projections but is not policy truth. | Repo docs identify `RUVIX_KERNEL_CONTRACT_v1` and policy events. Brain MCP search degraded during audit. | `data.policies`: `promotion_mode`, `auto_approval_threshold`, `roles`, `kernel_rules[]`, `isolation`, `locks`, `recent_policy_events[]`, `mutation_supported: false` for MVP; include `degraded`, `warnings`, `source`, `freshness`, `group_id`. | GET likely `viewer` for read-only status; any policy mutation must be `admin` and approval-gated with audit receipt. | Read-only MVP only after schema approval. No policy write/update/toggle route until mutation semantics, receipt fields, and Sabir approval exist. | If env/rule source missing, return `unknown` with warnings, not defaults presented as configured truth. If isolation check is unavailable, policy surface remains degraded. | Tests for env parsing without leaking secrets, role serialization, kernel rule source reporting, isolation degraded merge, auth, no mutation methods, and no fabricated defaults. | **Yes.** Define read-only policy status schema and explicitly separate it from policy mutation semantics. |
| `/api/memory/lineage` | **Blocked / read-only design needed** | No route file found at `src/app/api/memory/lineage`; inferred 404. Existing graph/traces/insights routes are partial and not full lifecycle lineage. | Join read model across PostgreSQL `events`, PostgreSQL `canonical_proposals.trace_ref`, approval audit events, Neo4j Memory/Insight nodes with `source_event_id` / `proposal_id`, and `SUPERSEDES` relationships. | `src/app/api/memory/graph/route.ts`; `src/app/api/memory/traces/route.ts`; `src/app/api/memory/insights/[id]/history/route.ts`; `src/lib/memory/detail-view.ts`; `src/lib/memory/provenance-export.ts`; `src/lib/validation/trace-ref.ts`; dashboard adapter entry. | Promotion/proposal Notion IDs exist as references; Notion is projection metadata in lineage, not the canonical chain. | Repo docs define trace -> proposal -> approval -> Neo4j -> retrieval as the lifecycle. Brain MCP search degraded during audit. | `data.lineage`: `memory_id?`, `proposal_id?`, `trace_ref?`, `nodes[]`, `edges[]`, `evidence[]`, `version_chain[]`, `missing_links[]`; each node includes `id`, `type`, `group_id`, `label`, `status`, `source`, `created_at`. | GET `viewer` for read-only lineage; enforce `group_id`; no writes. Trace evidence may need `curator` if raw metadata includes sensitive payloads. | GET only. Query by one of `memory_id`, `proposal_id`, or `trace_ref`. No graph mutation or repair in this route. | If Neo4j unavailable, return Postgres trace/proposal chain as degraded and mark semantic/version nodes unavailable. If Postgres unavailable, do not claim lineage from graph alone as complete. | Tests for each lookup mode, missing link reporting, Neo4j-degraded fallback, Postgres failure, tenant isolation, sensitive metadata redaction, SUPERSEDES chain inclusion, and no mutation methods. | **Yes.** Define lineage read model and allowed visibility for raw trace/proposal metadata. |

## Minimal Implementation Recommendations

| Route | Recommendation |
| --- | --- |
| `/api/events` | Implement only after Sabir chooses alias vs normalized feed. If alias is approved, reuse `queryAuditEvents()` and keep response source-labeled as PostgreSQL `events`. |
| `/api/contracts` | Do not implement live route yet. First write a contract schema ADR covering source precedence, visibility, and drift states. |
| `/api/promotions` | Safe narrow implementation: GET-only history over `canonical_proposals` plus approval/promotion event references. Leave all approval/reject/revoke commands on existing curator routes until mutation receipt schema is approved. |
| `/api/policies` | Do not implement as live green. First define read-only policy schema and distinguish runtime status from editable policy config. |
| `/api/memory/lineage` | Do not implement until Sabir approves the read model. After approval, build a read-only join adapter with explicit `missing_links` and degraded flags. |

Queen City rule: these are not live contracts yet. The repo has honest degraded adapter declarations, and that is better than mystery meat APIs.
