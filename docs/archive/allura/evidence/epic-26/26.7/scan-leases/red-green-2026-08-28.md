# Story 26.7 scan-lease slice — strict TDD evidence

> **AI assistance disclosure:** This implementation and evidence record were produced with AI assistance. Commands and salient output below are copied from the executed repository runs; no acceptance criterion or story status was changed.

Date: 2026-08-28. Branch: `feat/epic-26-story-26.7-upstream-plugin`.

## RED 1 — lease authority absent

Command:

```text
bun x vitest run src/lib/bumblebee/__tests__/lease-authority.test.ts
```

Salient failure (before production module existed):

```text
FAIL  src/lib/bumblebee/__tests__/lease-authority.test.ts
Error: Failed to load url ../lease-authority
Test Files  1 failed (1)
Tests  no tests
```

GREEN:

```text
✓ src/lib/bumblebee/__tests__/lease-authority.test.ts (1 test) 2ms
Test Files  1 passed (1)
Tests  1 passed (1)
```

## RED 2 — auth-before-body route handlers absent

Command:

```text
bun x vitest run src/lib/bumblebee/__tests__/lease-routes.test.ts
```

Salient failure:

```text
FAIL  src/lib/bumblebee/__tests__/lease-routes.test.ts
Error: Failed to load url ../lease-routes
Test Files  1 failed (1)
Tests  no tests
```

GREEN:

```text
✓ src/lib/bumblebee/__tests__/lease-routes.test.ts (1 test) 4ms
Test Files  1 passed (1)
Tests  1 passed (1)
```

## RED 3 — migration absent

Command:

```text
bun x vitest run src/lib/bumblebee/__tests__/scan-lease-migration.test.ts
```

Salient failure:

```text
ENOENT: no such file or directory, open '.../docker/postgres-init/47-bumblebee-scan-leases.sql'
Test Files  1 failed (1)
Tests  1 failed (1)
```

GREEN:

```text
✓ src/lib/bumblebee/__tests__/scan-lease-migration.test.ts (1 test) 2ms
Test Files  1 passed (1)
Tests  1 passed (1)
```

## RED 4 — opposite namespace was generic invalid, not exact audience denial

Command:

```text
bun x vitest run src/lib/bumblebee/__tests__/lease-authority.test.ts
```

Salient failure:

```text
expected [Function] to throw error including 'BUMBLEBEE_AUTH_AUDIENCE_FORBIDDEN' but got 'BUMBLEBEE_AUTH_INVALID'
Test Files  1 failed (1)
Tests  1 failed | 1 passed (2)
```

GREEN:

```text
✓ src/lib/bumblebee/__tests__/lease-authority.test.ts (2 tests) 4ms
Test Files  1 passed (1)
Tests  2 passed (2)
```

## Focused final verification

```text
bun x vitest run src/lib/bumblebee/__tests__/lease-authority.test.ts src/lib/bumblebee/__tests__/lease-routes.test.ts src/lib/bumblebee/__tests__/scan-lease-migration.test.ts src/lib/bumblebee/__tests__/source-authority.test.ts
```

```text
Test Files  4 passed (4)
Tests  14 passed (14)
```

```text
bun run typecheck
$ tsc --noEmit
```

Changed-file ESLint was executed directly because `bun run lint:ci` requires an explicit CI base SHA and exited 64 without one:

```text
bun x eslint --fix src/lib/bumblebee/lease-authority.ts src/lib/bumblebee/lease-repository.ts src/lib/bumblebee/lease-routes.ts src/lib/bumblebee/__tests__/lease-authority.test.ts src/lib/bumblebee/__tests__/lease-routes.test.ts src/lib/bumblebee/__tests__/scan-lease-migration.test.ts src/app/api/plugins/bumblebee/runs/route.ts src/app/api/plugins/bumblebee/ingest/route.ts src/__tests__/bumblebee-scan-leases.e2e.test.ts src/lib/db/tenant-table-inventory.ts vitest.config.live-db.ts
```

Exit code: `0`.

## Fresh PostgreSQL app-role proof

A new disposable `pgvector/pgvector:pg16` container was started on loopback port 5548 with no reused volume. Full migrations were applied in filename order:

```text
POSTGRES_HOST=127.0.0.1 POSTGRES_PORT=5548 POSTGRES_DB=memory POSTGRES_USER=allura POSTGRES_PASSWORD=[REDACTED] POSTGRES_APP_USER=allura_app POSTGRES_APP_PASSWORD=[REDACTED] bash scripts/ci/run-live-db-tests.sh --migrate-only --artifact-dir=artifacts/ci/story-26.7-scan-leases/live-db-fresh-2
```

Salient output:

```text
Applying 51 migrations in LC_ALL=C filename order.
Applying: 47-bumblebee-scan-leases.sql
COMMIT
vector:0.8.6
Migration-only validation completed.
```

Targeted proof command (password values redacted here; no raw token or secret was printed by the test):

```text
RUN_E2E_TESTS=true POSTGRES_HOST=127.0.0.1 POSTGRES_PORT=5548 POSTGRES_DB=memory POSTGRES_USER=allura POSTGRES_PASSWORD=[REDACTED] POSTGRES_APP_USER=allura_app POSTGRES_APP_PASSWORD=[REDACTED] bun x vitest run --config vitest.config.live-db.ts src/__tests__/bumblebee-scan-leases.e2e.test.ts
```

```text
✓ src/__tests__/bumblebee-scan-leases.e2e.test.ts (3 tests) 145ms
Test Files  1 passed (1)
Tests  3 passed (3)
```

The tests attest `current_user=allura_app`, `rolsuper=false`, `rolbypassrls=false`, table owner is not `allura_app`, ENABLE/FORCE RLS, no DELETE, scope-qualified FK failure, app-role inability to update generation/expiry, one-way revocation, cross-scope invisibility, and two concurrent generations exactly `[1,2]`. The trigger also protects owner-path mutations, but PostgreSQL superusers and object owners can alter schema/disable triggers; this proof does not claim protection from a hostile owner or superuser.

## Remediation cycle 1 — review and current-SHA CI

### RED — current-SHA structural CI failures

```text
bun x vitest run src/lib/bumblebee/__tests__/module.test.ts tests/scripts/validate-route-scope-manifest.test.ts
```

The module test failed `1 failed | 13 passed`: the two canonical headless API routes were incorrectly classified as foreign dashboard imports. The route validator separately reported both routes uncovered in current-SHA CI.

GREEN: the narrow module allowlist now names only those two canonical API routes, and the canonical manifest declares exact POST entries at the strongest host role while the handlers enforce server-derived plugin-token audiences.

```text
bun x vitest run src/lib/bumblebee/__tests__/module.test.ts
Tests  14 passed (14)

bun run validate:route-scope-manifest
Total routes found: 91
Covered by manifest: 78
Public (declared): 13
UNCOVERED (FAIL): 0
WEAK ROLE (FAIL): 0
PASS
```

### RED — DB insertion authority, credential race, and route disclosure

```text
bun x vitest run src/lib/bumblebee/__tests__/lease-routes.test.ts src/lib/bumblebee/__tests__/scan-lease-migration.test.ts
Test Files  2 failed (2)
Tests  5 failed | 6 passed (11)
```

Failures proved that `allura_app` still had direct INSERT, issuance was not SECURITY DEFINER with a minimal path/explicit owner and same-transaction credential revalidation, unknown auth/config/DB messages returned 401 with their raw text, and all issuance errors returned 409.

GREEN:

```text
bun x vitest run src/lib/bumblebee/__tests__/lease-routes.test.ts src/lib/bumblebee/__tests__/scan-lease-migration.test.ts
Test Files  2 passed (2)
Tests  11 passed (11)
```

The minimal fix revokes direct INSERT, grants exact function EXECUTE only, sets the three SECURITY DEFINER functions to explicit trusted owner `allura` with `search_path=pg_catalog`, locks/revalidates the canonical runner credential for audience/revocation/expiry using server time, adds the source-to-runner composite FK, and allowlists only known public route errors. Unknown failures now return stable `BUMBLEBEE_SERVICE_UNAVAILABLE` status 503 without internals.

### Fresh PostgreSQL full live lane

A disposable `pgvector/pgvector:pg16` container named `allura-story267-remediation` was destroyed and recreated before the final proof. Reusing the first seeded disposable database correctly failed setup on a duplicate credential; no test or setup was weakened. The final fresh command was:

```text
POSTGRES_HOST=127.0.0.1 POSTGRES_PORT=5551 POSTGRES_DB=memory POSTGRES_USER=allura \
POSTGRES_PASSWORD=[REDACTED] POSTGRES_APP_USER=allura_app POSTGRES_APP_PASSWORD=[REDACTED] \
bash scripts/ci/run-live-db-tests.sh --artifact-dir=artifacts/ci/story-26.7-remediation/live-db-fresh-final
```

Exact result from the JSON report:

```text
Applying 51 migrations in LC_ALL=C filename order.
Applying: 46-bumblebee-source-authority.sql
COMMIT
Applying: 47-bumblebee-scan-leases.sql
COMMIT
Test Suites: 28 passed, 0 failed, 28 total
Tests: 90 passed, 0 failed, 0 pending, 90 total
success: true
```

The expanded scan-lease live suite exercises the exported `/runs` POST through real `authenticateBumblebeeRequest`, `authenticateRunnerForSource`, HMAC verification, and `persistScanLease`; direct app-role INSERT denial; canonical population/catalog copying; expired/revoked bootstrap before body parsing; failed issuance rollback with generation reuse; and the deterministic interleaving where revocation holds the credential lock first, issuance waits, then produces no lease after revocation commits. It does not claim that revocation wins when issuance already owns the lock.

### Final static/unit gates

```text
bun x vitest run src/lib/bumblebee/__tests__/lease-authority.test.ts src/lib/bumblebee/__tests__/lease-routes.test.ts src/lib/bumblebee/__tests__/scan-lease-migration.test.ts src/lib/bumblebee/__tests__/source-authority.test.ts src/lib/bumblebee/__tests__/module.test.ts
Test Files  5 passed (5)
Tests  37 passed (37)

bun run test:unit
Test Files  126 passed | 6 skipped (132)
Tests  2205 passed | 160 skipped (2365)

bun run typecheck
$ tsc --noEmit
Exit code: 0

bun run lint:ci --base=822a53116d4f13c628493a7f6cf0cf1e5155465b
Linting 16 changed JavaScript/TypeScript file(s) against base 822a53116d4f13c628493a7f6cf0cf1e5155465b.
Exit code: 0 (zero warnings, zero errors)

git diff --check
Exit code: 0
```
